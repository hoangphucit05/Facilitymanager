{
  "name": "facility-manager-frontend",
  "version": "0.0.1",
  "private": true,
  "description": "Static multi-page UI for Facility Manager",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "npm run build && npx --yes serve dist -p 5173"
  }
}
