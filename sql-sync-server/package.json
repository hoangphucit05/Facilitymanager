{
  "name": "facility-manager-sql-sync",
  "version": "1.0.0",
  "description": "Proxy server: chuyển tiếp API tới Spring Boot và tự động export lại schema_main.sql sau mỗi thao tác CRUD",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "mysql2": "^3.9.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
